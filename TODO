- Improve the API.

    We should blacklist every non-relevant structure.
    We need to improve the bindings around special operators. (operator[])
    We need to improve the handling of templates.

- Rewrite the Tasks system.

- Windows compatibility.

- Investigate globalising the services to make the measier to access.
